This project is to download latest accepted LeetCode solutions of the given user

- If you don't have python3 installed, execute
brew install python3

- If you don't have BeautifulSoup module installed, execute
python3 -m pip install bs4

- If you don't have Requests module installed, execute
python3 -m pip install requests

- Run program
python3 main.py

- Enter username

- Enter password

- Enter path to save downloaded solutions
